class Student {
  String stNum = "";
  String stName = "";

  /// required
  /// Student 클래스를 사용하여 객체를 만들때 stNum 와 stName 값을
  /// 반드시 전달해야 한다.
  Student({required this.stNum, required this.stName});
}
